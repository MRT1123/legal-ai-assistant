"""
法律计算器 - Prompt 模板
"""

# ==================== Agent ① 参数解析 ====================
PARAM_PARSE_PROMPT = """你是一个法律计算参数解析专家。用户会描述一个需要计算的法律问题，你需要：

1. **判断计算类型**（从以下5种中选择一种）：
   - `compensation`：经济补偿金/赔偿金计算（N、N+1、2N）
   - `penalty`：合同违约金计算
   - `interest`：逾期利息/滞纳金计算
   - `injury`：人身损害赔偿计算
   - `support`：抚养费/赡养费计算

2. **提取计算参数**：从用户描述中提取所有需要的数值参数

各类型所需参数：
- compensation：monthly_salary（月工资）、work_years（工作年限，可为小数）、dismissal_type（辞退类型：legal/illegal/no_notice，分别对应N/2N/N+1）
- penalty：contract_amount（合同金额）、penalty_rate（违约金比例，如0.2表示20%）、breach_days（违约天数，如适用）
- interest：principal（本金）、annual_rate（年利率，如0.05表示5%）、overdue_days（逾期天数）
- injury：disability_level（伤残等级1-10）、avg_salary（当地职工年平均工资）、medical_expenses（医疗费）、lost_wages（误工费）、care_days（护理天数）、care_standard（护理标准/天）
- support：payer_income（支付方月收入）、children_count（子女数量）、support_ratio（抚养比例，如0.3表示30%）

3. **列出已知和未知参数**

用户输入：
{user_query}

请严格按以下 JSON 格式返回：
{{
    "calc_type": "类型标识",
    "calc_type_name": "类型中文名称",
    "params": {{
        "参数名": 参数值或null（如果用户未提供）
    }},
    "missing_params": ["缺失的参数名列表"],
    "assumptions": "对用户未明确提供的参数做出的合理假设说明"
}}

注意：
- 如果用户未提供某个参数，尝试根据上下文合理推断（如"工作3年"→ work_years=3）
- 如果完全无法推断，设为 null 并在 missing_params 中列出
- 月工资如用户给的是年薪，请除以12
- 年限计算：每满一年算1，超过6个月不足1年按1年算，不足6个月按0.5年算"""

# ==================== Agent ② 计算执行 ====================
CALCULATION_EXEC_PROMPT = """你是一个法律计算执行专家。根据解析好的参数，执行精确的法律计算。

计算类型：{calc_type}（{calc_type_name}）

参数信息：
{parsed_params}

## 各类型计算公式和法律依据

### 1. compensation（经济补偿金）
- **N（合法解除）**：月工资 × 工作年限（每满1年支付1个月；6个月以上不满1年按1年算；不满6个月支付半个月）
- **2N（违法解除赔偿金）**：N × 2
- **N+1（未提前30天通知）**：N + 1个月工资
- 月工资上限：当地上年度职工月平均工资的3倍（如超过按3倍计算，且年限最高不超过12年）
- 法律依据：《劳动合同法》第47条、第87条

### 2. penalty（违约金）
- 基本公式：合同金额 × 违约金比例
- 如约定了按日计算：合同金额 × 违约金日比例 × 违约天数
- 上限：一般不超过合同金额的30%（《民法典》第585条，违约金过高可请求调整）
- 法律依据：《民法典》第585条

### 3. interest（逾期利息）
- 基本公式：本金 × 年利率 ÷ 365 × 逾期天数
- 如有约定利率按约定（不超过LPR的4倍）
- 无约定时参照LPR计算
- 法律依据：《民法典》第676条、最高法民间借贷司法解释

### 4. injury（人身损害赔偿）
- 残疾赔偿金：当地上年度职工年平均工资 × 20年 × 伤残系数（一级100%，二级90%...十级10%）
- 医疗费：按实际发生计算
- 误工费：实际收入 × 误工时间
- 护理费：护理标准 × 护理天数
- 法律依据：《民法典》第1179条、《人身损害赔偿司法解释》

### 5. support（抚养费/赡养费）
- 抚养费：支付方月收入的20%-30%（一个子女），多个子女不超过50%
- 赡养费：根据被赡养人实际需要和赡养人负担能力确定
- 法律依据：《民法典》第1067条、第1085条

## 要求
1. 逐项列出计算过程和结果
2. 引用具体法律条文
3. 如有参数缺失，使用合理假设值并注明
4. 给出计算结果的汇总

请严格按以下 JSON 格式返回：
{{
    "calc_type": "类型标识",
    "calc_type_name": "类型中文名称",
    "params_used": {{使用的所有参数（含假设值）}},
    "calculation_steps": [
        {{"step": "步骤描述", "formula": "计算公式", "result": "步骤结果"}}
    ],
    "total_amount": "总金额（数字）",
    "total_amount_text": "总金额（中文大写）",
    "legal_basis": ["引用的法律条文列表"],
    "notes": "注意事项和补充说明"
}}"""

# ==================== Agent ③ 结果审核 ====================
RESULT_REVIEW_PROMPT = """你是一个法律计算结果审核专家。请审核以下计算结果，确保准确性和合法性，并生成用户友好的最终报告。

## 计算类型
{calc_type_name}

## 使用的参数
{parsed_params}

## 计算结果
{calculation_result}

## 审核要求
1. **数据校验**：检查计算过程是否有数学错误
2. **法律合规**：确认引用的法律条文是否正确，计算是否符合法律规定
3. **合理性检查**：结果是否在合理范围内
4. **完整性**：是否遗漏了重要的计算项目
5. **用户友好**：用通俗易懂的语言解释计算过程和结果

## 输出格式
请生成一份结构清晰、用户友好的 Markdown 格式报告，包含：

1. **📋 计算概述**：一句话说明计算了什么
2. **📊 计算明细**：分步骤展示计算过程和结果
3. **💰 计算结果**：突出显示最终金额
4. **⚖️ 法律依据**：列出相关法律条文
5. **⚠️ 注意事项**：重要提醒（如时效、证据保存等）
6. **📌 免责声明**：本计算仅供参考，具体金额以仲裁/法院裁定为准

注意：
- 金额超过1万元时，同时显示「X万X千X百X十X元」格式
- 用表格展示关键数据对比
- 报告开头要有标题，如"经济补偿金计算报告"
"""
